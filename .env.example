# Absolute path to the development theme directory inside a test Vault.
# The folder should be dedicated to this theme because build output is copied here.
OBSIDIAN_THEME_DIR=/absolute/path/to/vault/.obsidian/themes/Pixel Workbench
